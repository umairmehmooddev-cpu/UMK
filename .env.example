# Public site origin. Safe to expose to the browser. Not a secret.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only secrets must not use the NEXT_PUBLIC_ prefix.
# Do not add GEMINI_API_KEY, database URLs, or session secrets until a server
# module in a later slice reads them. Never reference them from Client Components.
